name: Quality Checks

on:
  push:
  pull_request:

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run Prettier check
        run: bun run lint:prettier

      - name: Run ESLint
        run: bunx eslint .

      - name: Type check
        run: bunx tsc --noEmit

      - name: Run best practice checks
        run: bun run check:best
